package com.grupo3.technova.model.enums;

// Enum que representa las categorías posibles de un producto.
// Al usar un enum en vez de un String libre, el compilador nos avisa si escribimos mal un rol
public enum EnumCategoria {
    PERIFERICOS,
    COMPONENTES,
    REDES,
    SOFTWARE
}